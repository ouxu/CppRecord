vector<vector<int> > levelOrder(TreeNode *root) {
  vector<vector<int>> result;
  if (roor == NULL) {
    return result;
  }

  queue<TreeNode*> q;
  q.push(root);
  int count = 1;
  int level = 0;

  vector<int> tmp(0);
  while (!q.empty()) {
    tmp.clear();
    
  }



}
